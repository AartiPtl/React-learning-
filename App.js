import { StyleSheet, Button, Text, View, TextInput } from 'react-native';
import Goal from './src/goal/Goal';
import Layout from './src/goal/Layout';

export default function App() {
  return (
    <Layout/>
  );
}